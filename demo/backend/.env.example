# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=family_meals
DB_PORT=3306

# JWT Secret (change in production, must be at least 32 characters)
JWT_SECRET=your-secret-key-change-in-production-min-32-chars-long

# Server Port
PORT=3000

# Environment
NODE_ENV=development

# CORS (optional, defaults to *)
# CORS_ORIGIN=http://localhost:5173
